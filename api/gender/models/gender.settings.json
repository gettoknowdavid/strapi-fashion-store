{
  "kind": "collectionType",
  "collectionName": "genders",
  "info": {
    "name": "Gender"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string"
    },
    "slug": {
      "type": "uid",
      "targetField": "title"
    },
    "types": {
      "via": "genders",
      "collection": "type"
    }
  }
}
